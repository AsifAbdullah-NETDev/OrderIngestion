name: CI/CD to Render

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout repo
      - name: Checkout code
        uses: actions/checkout@v4

      # 2. Setup .NET 8
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      # 3. Restore & Build
      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore --configuration Release

      # 4. Run Tests
      - name: Run Tests
        run: dotnet test --no-build --configuration Release --verbosity normal

      # 5. Lint / Format Check (optional)
      - name: Lint / Format
        run: dotnet format --verify-no-changes || true

      # 6. Build Docker Image
      - name: Build Docker image
        run: |
          docker build -t ghcr.io/<org>/order-ingestion-api:latest -f ./OrderIngestion.Api/Dockerfile .

      # 7. Push Docker Image (optional)
      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Push Docker image
        run: docker push ghcr.io/<org>/order-ingestion-api:latest

      # 8. Trigger Render Deploy Hook
      - name: Deploy to Render
        run: curl -X POST "$RENDER_DEPLOY_HOOK"
        env:
          RENDER_DEPLOY_HOOK: ${{ secrets.RENDER_DEPLOY_HOOK }}
